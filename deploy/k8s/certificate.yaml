apiVersion: cert.gardener.cloud/v1alpha1
kind: Certificate
metadata:
  name: medimap-tls
  namespace: medimap
spec:
  commonName: medimap.elalwany.de
  dnsNames:
    - medimap.elalwany.de
  secretName: medimap-tls
